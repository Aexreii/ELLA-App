from app import app

# Vercel needs the application instance to be named 'app'
# Since it's already named 'app' in app.py, we just import it.
# This file acts as the entry point for Vercel Serverless Functions.
